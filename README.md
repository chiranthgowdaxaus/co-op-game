# Co-op game foundation

```bash
npm install
npm run dev
```

Open `http://localhost:5173` in two browser tabs. Create a room in one tab, then join its code from the other. Move with WASD or the arrow keys.
